Let S be a set of mapped points in \rm I\!R^2 \n

\forall \vec{p} \in S \exists \vec{r} : (\vec{p} + \vec{r}) \in S \n

Let \vec{x} be the position of a car, and let \vec{v} be it's velocity at any instant \n

If \ (\vec{x} + \vec{v}) \ intersects \ (\vec{p} + \vec{r}) \ then \ \exists(t,u) : (\vec{p} + t\vec{r}) = (\vec{x} + u\vec{v}), \ \{t,u \in \rm I\!R | \ 0 < t,u \leq 1\}

Solving \ for \ t \ and \ u \ gives: \n

t = \bigg|\bigg[\begin{matrix} (x_1 - p_1) & v_1 \\ (x_2 - p_2) & v_2 \end{matrix}\bigg]\bigg|\cdot \bigg|\bigg[\begin{matrix} r_1 & v_1 \\ r_2 & v_2 \end{matrix}\bigg]\bigg|